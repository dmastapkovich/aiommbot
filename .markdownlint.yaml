# Structural rules for every Markdown file in the catalogue.
#
# The shape is astral-sh/ruff's and astral-sh/ty's -- start from every rule and
# subtract -- with pydantic/pydantic's habit of a reason above each override,
# which is what lets a later reader re-enable a rule safely.
#
# Four rules the corpus disables are kept on here, because the reason the corpus
# gives does not transfer:
#   MD041 first-line-h1   ruff, ty, pydantic, OWASP, open-telemetry and trallnag
#                         all disable it because mkdocs inserts the H1. Nothing
#                         generates our files; each one opens with its own title.
#   MD046 code-block-style ruff and pydantic disable it for mkdocs-material
#                         content tabs and PyMdown admonitions. We have neither,
#                         and OWASP/ASVS keeps it pinned to fenced for nothing.
#   MD059 link-text       ruff disables it because "here" is convenient.
#                         documentation-style.md §6 requires the opposite: link
#                         text names the target.
#   MD007 ul-indent       four of the six corpus configs set indent 4, which is
#                         what mkdocs needs. All 13 nested list items in this
#                         catalogue use 2, which is the rule's own default.

default: true

# MD013/line-length
# Width is documentation-style.md §8 and `.agents/scripts/check-docs.py --width`
# carries its exemptions: a fence, front matter, a table row, a heading, a row
# holding one record, and a line whose overflow has no whitespace to wrap at.
# All six markdownlint configurations in the reference corpus disable MD013 too.
MD013: false

# MD024/no-duplicate-heading
# An ADR repeats "Considered options" under different parents, and an arc42
# section repeats a scenario heading under different views. Six of six corpus
# configurations make the same relaxation.
MD024:
  siblings_only: true

# MD033/no-inline-html
# Six occurrences, every one an arc42 template placeholder quoted verbatim in a
# documentation-engineering note -- `| _<Q-goal 1>_ | _<Text>_ |`. Rewriting
# them edits quoted material, which is the same reason §8 exempts a note from
# the width rule.
MD033: false

# MD034/no-bare-urls
# 491 occurrences, almost all inside sentences a research note quotes from its
# source. §6 forbids a bare URL in prose we write; a quotation is not prose we
# write, and wrapping one in a link changes what the source said. OWASP/ASVS is
# the only corpus configuration that touches this rule, and it disables it.
MD034: false
