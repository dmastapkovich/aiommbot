# 6. Runtime view

_Status: not started._

Sequence diagrams of the scenarios that matter: bot start-up and plugin activation; a post arriving over WebSocket and reaching a handler; an interactive button callback via webhook; reconnect with resume; a state transition with event isolation; graceful shutdown and drain; a failure inside a handler. Each scenario links to the component documents it crosses.
