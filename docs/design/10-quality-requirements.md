# 10. Quality requirements

_Status: not started._

Quality tree and concrete quality scenarios (stimulus → response → measure) for reliability, correctness, testability, maintainability, performance and security, e.g. 'the server closes the socket during a burst of 1,000 posts → the bot resumes within N seconds and processes each post exactly once'.
