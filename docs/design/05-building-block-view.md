# 5. Building block view

_Status: not started._

The static decomposition: C4 **Container** and **Component** diagrams, one level at a time (system → containers → components), with a table per level naming each block, its responsibility and its component document under `components/`. This section is the inventory from which the `LLD: <component>` tickets are generated.
