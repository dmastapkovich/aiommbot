# 4. Solution strategy

_Status: not started._

The handful of decisions that shape everything else: small core with an explicit platform boundary, plugin model, event model, execution model (async core, generated sync face), fault-tolerance strategy for the gateway, quality-by-mechanism. Each item is one paragraph linking to its ADR.
